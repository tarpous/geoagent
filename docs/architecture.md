# Architecture

This document will describe when the swarm path outperforms the single-agent baseline, backed by measured local-model results.

Until M3–M5 land, treat `AGENTS.md` and `05-geoagent-agentic-rag.md` as the architecture source of truth.
